class ModelRoute {
  final int id;
  final String name;
  final int min;
  final String addedDate;
  final String date;
  final bool isPlanned;

  ModelRoute(this.id, this.name, this.min, this.addedDate, this.date, this.isPlanned);
}