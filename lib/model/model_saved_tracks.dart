class ModelSavedTracks {
  final int id;
  final String name;
  final int min;
  final String trackedDate;
  final String duration;

  ModelSavedTracks(this.id, this.name, this.min, this.trackedDate, this.duration);

}